import { exclude } from './functions/exclude';
import { include } from './functions/include';
import { sort_by } from './functions/sort';

export const sortControllers = {
  sort_by,
  include,
  exclude,
};
